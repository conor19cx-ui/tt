<template>
	<view class="page">
		<view class="item" @click="goApply('selfroom')">
			<image class="icon-img" src="/static/自习室.png" mode="aspectFit"></image>
			<view class="info">
				<text class="name">自习室预约</text>
				<text class="desc">预约图书馆或教学楼自习座位</text>
			</view>
		</view>

		<view class="item" @click="goApply('repair')">
			<image class="icon-img" src="/static/宿舍.png" mode="aspectFit"></image>
			<view class="info">
				<text class="name">宿舍报修</text>
				<text class="desc">提交宿舍设备设施故障报修</text>
			</view>
		</view>

		<view class="item" @click="goApply('errands')">
			<image class="icon-img" src="/static/跑腿.png" mode="aspectFit"></image>
			<view class="info">
				<text class="name">跑腿代取</text>
				<text class="desc">代取快递，上门送达宿舍</text>
			</view>
		</view>

		<view class="my-link" @click="goMy">
			<text class="my-text">我的预约</text>
		</view>
	</view>
</template>

<script setup>
	const goApply = (type) => { uni.navigateTo({ url: '/pages/service/apply?type=' + type }); };
	const goMy = () => { uni.navigateTo({ url: '/pages/service/my' }); };
</script>

<style>
	.page {
		min-height: 100vh;
		background-color: #f5f5f5;
		padding-top: 30rpx;
		padding-left: 30rpx;
		padding-right: 30rpx;
	}

	.item {
		background-color: #ffffff;
		border-radius: 24rpx;
		padding-top: 36rpx;
		padding-bottom: 36rpx;
		padding-left: 36rpx;
		padding-right: 36rpx;
		flex-direction: row;
		align-items: center;
		margin-bottom: 30rpx;
	}

	.icon-img {
		width: 120rpx;
		height: 120rpx;
	}

	.info {
		flex: 1;
		margin-left: 30rpx;
	}

	.name {
		font-size: 32rpx;
		font-weight: bold;
		color: #333333;
	}

	.desc {
		font-size: 26rpx;
		color: #999999;
		margin-top: 10rpx;
	}

	.my-link {
		background-color: #ffffff;
		border-radius: 24rpx;
		padding-top: 30rpx;
		padding-bottom: 30rpx;
		align-items: center;
		margin-top: 40rpx;
	}

	.my-text {
		font-size: 30rpx;
		color: #007AFF;
	}
</style>
